// Note: FOTitle = First-order title (equivalent to h1)
import styled from 'styled-components'

const FOTitle = styled.h1`
  line-height: 32px;
  font-family: 'Fraunces', serif;
  font-size: 32px;
`;

export default FOTitle;